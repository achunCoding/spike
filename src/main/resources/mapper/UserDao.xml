<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wycfight.spike.dao.UserDao">

    <select id="getUserByName" resultType="top.wycfight.spike.entity.User">
        select * from spike_user where username = #{username}
    </select>
</mapper>